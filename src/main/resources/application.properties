cd C:\java-backend1

@'
# Supabase Database Configuration - Using Connection Pooler (More Reliable)
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.hptbymbybvqceyglkvtx
spring.datasource.password=Palagiri677245
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.keepalive-time=60000

# Server
server.port=8080

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.security=DEBUG
'@ | Out-File -FilePath src\main\resources\application.properties -Encoding UTF8

Write-Host "✅ Updated application.properties with connection pooler!" -ForegroundColor Green